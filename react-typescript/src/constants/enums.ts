export enum CellStatus {
  OCCUPIED = "OCCUPIED",
  AVAILABLE = "AVAILABLE",
}

export enum PlayerColour {
  BLACK = "BLACK",
  WHITE = "WHITE",
}

export enum GameStatus {
  WIN = "WIN",
  LOSE = "LOSE",
  DRAW = "DRAW",
  CONTINUE = "CONTINUE"
}