export enum CellStatus {
  OCCUPIED = "OCCUPIED",
  AVAILABLE = "AVAILABLE",
}

export enum PlayerColour {
  BLACK = "BLACK",
  WHITE = "WHITE",
}

export enum GameStatus {
  WIN = "WIN",
  DRAW = "DRAW",
  LOSE = "LOSE",
  NOT_OVER = "NOT OVER",
  OVER = "OVER",
}
